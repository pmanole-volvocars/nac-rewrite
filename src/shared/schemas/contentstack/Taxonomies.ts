/** Existing, already defined taxonomies and their unique IDs. */
export const Taxonomies = {
  /** A taxonomy of news categories. */
  // NOTE: Maybe we should have given this a better name (in CS),
  // like, I don't know... news_categories? :shrug:
  NewsCategories: "content_categories",
} as const
