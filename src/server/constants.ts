/**
 * Content Type UIDs for all content types found in Contentstack, on the `main` (production)
 * branch.
 */
export const CsContentTypeUids = {
  /** News entries. */
  News: "news",
  /** Documents entries. */
  Documents: "documents",
} as const
